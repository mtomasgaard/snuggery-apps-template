# For scripts/garmin_pull.py — the direct pull from Garmin Connect — and for
# scripts/make_demo_running_dashboard.py --tiles (requests only).
#
# Floors, not pins, on purpose: Garmin changes its sign-in flow a few times a
# year and the fix arrives as a new garminconnect release, so a pinned version
# would keep failing until someone edited this file. The trade is that every
# pull installs whatever is newest, in a job holding a live Garmin session. If
# you would rather review updates before they run, pin each line with == and
# let Dependabot open the bumps.
garminconnect>=0.3
fitdecode>=0.10
requests>=2.31

# Global Wind needs two more — `eccodes` (the GRIB decoder, wheel included, so
# nothing to apt-get) and `numpy` — but they are deliberately NOT listed above.
# This file is installed by the Running Dashboard's pull workflow, which has no
# use for a GRIB decoder, so refresh-global-wind.yml installs its own two
# directly:
#
#     python3 -m pip install eccodes numpy
#
# Every other script in this folder is standard library only.
